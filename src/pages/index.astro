---
import Layout from '../layouts/Layout.astro';
import { config } from '../config';
---

<Layout title={config.title}>
	<section class="hero">
		<h1>{config.title}</h1>
		<p class="tagline">{config.description}</p>
	</section>

	<section class="intro">
		<h2>Welcome</h2>
		<p>{config.author.bio}</p>
		<p>This site is built with Astro and deployed on Cloudflare Workers, featuring the bold typography and minimal design of the Volks Typo theme.</p>
	</section>

	<section class="features">
		<h2>Features</h2>
		<ul>
			<li>Lightning-fast performance with edge deployment</li>
			<li>Bold, uppercase typography for impact</li>
			<li>Minimal, monotone design with red accents</li>
			<li>Dark mode support (coming soon)</li>
			<li>Blog with MDX support</li>
		</ul>
	</section>

	<section class="cta">
		<h2>Get Started</h2>
		<p>Check out the <a href="/blog">blog</a> to see the latest posts, or explore the site to learn more about me and my work.</p>
	</section>
</Layout>

<style>
	.hero {
		text-align: center;
		padding: calc(var(--grid-unit) * 8) 0;
		border-bottom: 4px solid var(--color-accent);
		margin-bottom: calc(var(--grid-unit) * 6);
	}

	.hero h1 {
		font-size: 3.5rem;
		margin-bottom: calc(var(--grid-unit) * 2);
	}

	.tagline {
		font-size: 1.25rem;
		color: var(--color-text-secondary);
		font-weight: 500;
		letter-spacing: 0.05em;
	}

	section {
		margin-bottom: calc(var(--grid-unit) * 6);
	}

	.features ul {
		list-style: none;
		padding: 0;
	}

	.features li {
		padding-left: calc(var(--grid-unit) * 3);
		position: relative;
		margin-bottom: calc(var(--grid-unit) * 2);
	}

	.features li::before {
		content: '▪';
		position: absolute;
		left: 0;
		color: var(--color-accent);
		font-size: 1.5rem;
		line-height: 1;
	}

	.cta {
		text-align: center;
		padding: calc(var(--grid-unit) * 4);
		background-color: var(--color-surface);
		border: 2px solid var(--color-border);
	}
</style>
