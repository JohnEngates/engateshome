---
import Layout from '../layouts/Layout.astro';
import { config } from '../config';
import { Image } from 'astro:assets';
import profileImage from '../assets/images/site/engates1x1.png';
---

<Layout title={config.title}>
	<section class="hero">
		<div class="hero-content">
			<Image 
				src={profileImage} 
				alt={config.author.name} 
				class="hero-avatar"
				width={150}
				height={150}
				format="webp"
				quality={85}
			/>
			<h1>{config.title}</h1>
			<p class="tagline">{config.description}</p>
		</div>
	</section>

	<section class="intro">
		<h2>Exploring AI's Next Frontier</h2>
		<p>{config.author.bio}</p>
	</section>

	<section class="research-areas">
		<h2>Research Focus</h2>
		<div class="research-grid">
			<div class="research-item">
				<h3>Agentic AI</h3>
				<p>Building autonomous AI agents that can reason, plan, and act independently while maintaining alignment with human goals.</p>
			</div>
			<div class="research-item">
				<h3>Multi-Agent Systems</h3>
				<p>Exploring how swarms of AI agents collaborate, communicate, and coordinate to solve complex problems beyond individual capabilities.</p>
			</div>
			<div class="research-item">
				<h3>Real-World Applications</h3>
				<p>Understanding emergent behaviors and collective intelligence to solve challenges in cybersecurity, distributed computing, and autonomous systems.</p>
			</div>
			<div class="research-item">
				<h3>Cybersecurity & AI Defense</h3>
				<p>Leveraging AI agents for proactive threat detection, automated incident response, and adaptive security systems that evolve to counter emerging cyber threats.</p>
			</div>
			<div class="research-item">
				<h3>Machine Identity & Zero Trust</h3>
				<p>Securing the 45:1 ratio of machines to humans in modern enterprises. From service accounts to AI agents, every non-human actor needs identity verification, policy enforcement, and lifecycle management to prevent the next major breach.</p>
			</div>
			<div class="research-item">
				<h3>Quantum-Safe AI Systems</h3>
				<p>Preparing AI infrastructure for the post-quantum era by exploring quantum-resistant algorithms and the intersection of quantum computing with distributed AI systems.</p>
			</div>
		</div>
	</section>

	<section class="company-logos">
		<h2>Executive Leadership</h2>
		<p class="logos-subtitle">CTO roles at companies that shaped the modern internet</p>
		<div class="logos-grid">
			<a href="https://www.rackspace.com" target="_blank" rel="noopener noreferrer" class="company-logo" aria-label="Rackspace">
				<span class="logo-text rackspace">rackspace</span>
				<span class="role-title">CTO</span>
			</a>
			<a href="https://www.cloudflare.com" target="_blank" rel="noopener noreferrer" class="company-logo" aria-label="Cloudflare">
				<span class="logo-text cloudflare">Cloudflare</span>
				<span class="role-title">Field CTO</span>
			</a>
		</div>
	</section>

	<section class="security-quote">
		<blockquote>
			<p>"I've always believed that security isn't an add-on - it's the foundation for scalable AI. As AI agents take on more mission-critical workflows across the enterprise...we must all raise standards for security and data protection, while continuing to advance the frontier of enterprise-grade AI."</p>
		</blockquote>
	</section>

	<section class="experience">
		<h2>Building Tomorrow's Infrastructure</h2>
		<p>With three decades of experience building and securing the systems that power the Internet—from pioneering cloud computing at Rackspace to protecting the digital edge at Cloudflare—I bring a unique perspective to AI research. My cybersecurity expertise informs my approach to building resilient AI systems, while my exploration of quantum computing prepares these systems for tomorrow's computational paradigms. The same principles that made cloud computing ubiquitous now guide my work at the intersection of AI, security, and quantum-ready infrastructure.</p>
		<div class="cta-buttons">
			<a href="/about" class="cta-btn primary">Learn More About Me</a>
			<a href="/blog" class="cta-btn secondary">Read My Latest Thoughts</a>
		</div>
	</section>

	<section class="connect">
		<h2>CONNECT WITH ME</h2>
		<div class="social-links">
			<a href={config.social.linkedin} class="social-link" target="_blank" rel="noopener noreferrer">
				<i class="fab fa-linkedin"></i> LINKEDIN
			</a>
			<a href={config.social.twitter} class="social-link" target="_blank" rel="noopener noreferrer">
				<i class="fab fa-x-twitter"></i> X
			</a>
			<a href={config.social.github} class="social-link" target="_blank" rel="noopener noreferrer">
				<i class="fab fa-github"></i> GITHUB
			</a>
			<a href={`mailto:${config.social.email}`} class="social-link">
				<i class="fas fa-envelope"></i> EMAIL
			</a>
		</div>
	</section>

</Layout>

<style>
	.hero {
		text-align: center;
		padding: calc(var(--grid-unit) * 8) 0;
		border-bottom: 4px solid var(--color-accent);
		margin-bottom: calc(var(--grid-unit) * 6);
	}

	.hero-content {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: calc(var(--grid-unit) * 3);
	}

	.hero-avatar {
		width: 150px;
		height: 150px;
		border-radius: 50%;
		border: 4px solid var(--color-border);
		box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
		object-fit: cover;
	}

	.hero h1 {
		font-size: 2.5rem;
		margin-bottom: calc(var(--grid-unit) * 2);
	}

	.tagline {
		font-size: 1.25rem;
		color: var(--color-text-secondary);
		font-weight: 500;
		letter-spacing: 0.05em;
		max-width: 600px;
		line-height: 1.6;
	}

	section {
		margin-bottom: calc(var(--grid-unit) * 8);
	}

	.intro p {
		font-size: 1.125rem;
		line-height: 1.8;
		max-width: 900px;
		margin: 0 auto;
	}

	.research-areas {
		background-color: var(--color-surface);
		padding: calc(var(--grid-unit) * 6);
		border-radius: calc(var(--grid-unit));
	}

	.research-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
		gap: calc(var(--grid-unit) * 4);
		margin-top: calc(var(--grid-unit) * 4);
	}

	.research-item {
		padding: calc(var(--grid-unit) * 3);
		background-color: var(--color-background);
		border: 2px solid var(--color-border);
		border-radius: calc(var(--grid-unit));
		transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1),
		            box-shadow 0.3s cubic-bezier(0.4, 0, 0.2, 1),
		            border-color 0.3s ease;
	}

	.research-item:hover {
		border-color: var(--color-accent);
		transform: translateY(-6px);
		box-shadow: 0 12px 24px rgba(0, 0, 0, 0.12);
	}

	[data-theme="dark"] .research-item:hover {
		box-shadow: 0 12px 24px rgba(0, 0, 0, 0.4);
	}

	.research-item h3 {
		font-size: 1.25rem;
		margin-bottom: calc(var(--grid-unit) * 2);
	}

	.research-item p {
		font-size: 1rem;
		line-height: 1.6;
		color: var(--color-text-secondary);
	}

	.company-logos {
		text-align: center;
		padding: calc(var(--grid-unit) * 6) 0;
		margin: calc(var(--grid-unit) * 4) 0;
	}

	.logos-subtitle {
		font-size: 1.125rem;
		color: var(--color-text-secondary);
		margin-bottom: calc(var(--grid-unit) * 5);
	}

	.logos-grid {
		display: flex;
		justify-content: center;
		align-items: center;
		gap: calc(var(--grid-unit) * 8);
		flex-wrap: wrap;
	}

	.company-logo {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: calc(var(--grid-unit) * 1.5);
		text-decoration: none;
		padding: calc(var(--grid-unit) * 3);
		border-radius: calc(var(--grid-unit));
		transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1),
		            opacity 0.3s ease;
		opacity: 0.6;
		filter: grayscale(100%);
	}

	.company-logo:hover {
		opacity: 1;
		filter: grayscale(0%);
		transform: translateY(-4px);
		text-decoration: none;
	}

	.logo-text {
		font-family: var(--font-heading-primary);
		font-size: 2.5rem;
		font-weight: 700;
		letter-spacing: 0.02em;
		color: var(--color-text-primary);
		transition: color 0.3s ease;
	}

	.logo-text.rackspace {
		text-transform: lowercase;
		letter-spacing: 0.05em;
	}

	.company-logo:hover .logo-text.rackspace {
		color: #c8102e;
	}

	.company-logo:hover .logo-text.cloudflare {
		color: #f38020;
	}

	.role-title {
		font-family: var(--font-heading-secondary);
		font-size: 0.875rem;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.1em;
		color: var(--color-text-muted);
	}

	@media (max-width: 768px) {
		.logos-grid {
			gap: calc(var(--grid-unit) * 4);
		}

		.logo-text {
			font-size: 2rem;
		}
	}

	.experience {
		text-align: center;
	}

	.experience p {
		font-size: 1.125rem;
		line-height: 1.8;
		max-width: 800px;
		margin: 0 auto calc(var(--grid-unit) * 4) auto;
	}

	.cta-buttons {
		display: flex;
		gap: calc(var(--grid-unit) * 3);
		justify-content: center;
		flex-wrap: wrap;
		margin-top: calc(var(--grid-unit) * 4);
	}

	.cta-btn {
		font-family: var(--font-heading-secondary);
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		padding: calc(var(--grid-unit) * 2) calc(var(--grid-unit) * 4);
		border-radius: 4px;
		transition: transform 0.2s cubic-bezier(0.4, 0, 0.2, 1),
		            box-shadow 0.2s cubic-bezier(0.4, 0, 0.2, 1),
		            background-color 0.2s ease,
		            border-color 0.2s ease,
		            color 0.2s ease;
		display: inline-block;
	}

	.cta-btn.primary {
		background-color: var(--color-accent);
		color: white;
		border: 2px solid var(--color-accent);
	}

	.cta-btn.primary:hover {
		background-color: var(--color-text-primary);
		border-color: var(--color-text-primary);
		text-decoration: none;
		transform: translateY(-2px);
		box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
	}

	.cta-btn.secondary {
		background-color: transparent;
		color: var(--color-text-primary);
		border: 2px solid var(--color-border);
	}

	.cta-btn.secondary:hover {
		border-color: var(--color-accent);
		color: var(--color-accent);
		text-decoration: none;
		transform: translateY(-2px);
		box-shadow: 0 4px 8px rgba(0, 0, 0, 0.05);
	}

	.security-quote {
		margin: calc(var(--grid-unit) * 10) auto;
		max-width: 900px;
		padding: 0 calc(var(--grid-unit) * 4);
	}

	.security-quote blockquote {
		margin: 0;
		padding: calc(var(--grid-unit) * 5);
		background-color: var(--color-surface);
		border-left: 6px solid var(--color-accent);
		position: relative;
	}

	.security-quote blockquote::before {
		content: '"';
		position: absolute;
		top: -20px;
		left: 20px;
		font-size: 5rem;
		color: var(--color-accent);
		font-family: var(--font-heading);
		line-height: 1;
		opacity: 0.3;
	}

	.security-quote p {
		margin: 0;
		font-size: 1.25rem;
		line-height: 1.8;
		font-style: italic;
		color: var(--color-text-primary);
		font-weight: 500;
	}

	.connect {
		text-align: center;
		padding: calc(var(--grid-unit) * 8) 0 calc(var(--grid-unit) * 4) 0;
		border-top: 4px solid var(--color-accent);
		margin-top: calc(var(--grid-unit) * 8);
	}

	.connect h2 {
		font-size: 2.5rem;
		margin-bottom: calc(var(--grid-unit) * 6);
		letter-spacing: 0.05em;
	}

	.social-links {
		display: flex;
		gap: calc(var(--grid-unit) * 3);
		justify-content: center;
		flex-wrap: wrap;
	}

	.social-link {
		font-family: var(--font-heading-secondary);
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		padding: calc(var(--grid-unit) * 1.5) calc(var(--grid-unit) * 3);
		border: 2px solid var(--color-text-primary);
		background-color: transparent;
		color: var(--color-text-primary);
		transition: all 0.2s ease;
		display: inline-flex;
		align-items: center;
		gap: calc(var(--grid-unit));
		font-size: 0.875rem;
	}

	.social-link i {
		font-size: 1rem;
	}

	.social-link:hover {
		border-color: var(--color-accent);
		color: var(--color-accent);
		text-decoration: none;
		transform: translateY(-2px);
		box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
	}

	@media (max-width: 768px) {
		.hero h1 {
			font-size: 2.5rem;
		}

		.hero-avatar {
			width: 120px;
			height: 120px;
		}

		.research-areas {
			padding: calc(var(--grid-unit) * 4) calc(var(--grid-unit) * 2);
		}

		.research-grid {
			grid-template-columns: 1fr;
		}

		.security-quote {
			padding: 0 calc(var(--grid-unit) * 2);
		}

		.security-quote blockquote {
			padding: calc(var(--grid-unit) * 3);
		}

		.security-quote p {
			font-size: 1.125rem;
		}

		.social-links {
			grid-template-columns: 1fr 1fr;
			display: grid;
			gap: calc(var(--grid-unit) * 2);
			padding: 0 calc(var(--grid-unit) * 2);
		}
	}
</style>
